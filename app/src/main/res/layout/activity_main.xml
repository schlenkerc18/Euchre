<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/felt_background"
    tools:context="com.hfad.euchreai.MainActivity">

    <Button
        android:id="@+id/activity_game"
        android:onClick="onGameActivity"
        android:layout_marginTop="60dp"
        android:layout_width="140dp"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:text="Play!" />

    <Button
        android:id="@+id/activity_stats"
        android:onClick="onStatsActivity"
        android:layout_below="@+id/activity_game"
        android:layout_width="140dp"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:text="Statistics" />

    <Button
        android:id="@+id/activity_rulebook"
        android:onClick="onRulebookActivity"
        android:layout_below="@+id/activity_stats"
        android:layout_width="140dp"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:text="Rulebook" />
</RelativeLayout>
